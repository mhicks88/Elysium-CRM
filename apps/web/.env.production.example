# =============================================================================
# ELYSIUM CRM — WEB (VITE) PRODUCTION ENVIRONMENT TEMPLATE
# Copy to ".env.production" for production deployment
# =============================================================================

# Base URL for the API — MUST include https:// in production.
# Example if deploying API behind an AWS load balancer:
#   https://api.elysiumcrm.com
# -----------------------------------------------------------------------------
VITE_API_BASE_URL=


