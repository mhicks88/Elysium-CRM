describe("runPreCallChecks", () => {
  test.todo("returns FAIL when lead status is DO_NOT_CONTACT");
  test.todo(
    "returns FAIL for marketing when permissionToContactPhone is false"
  );
  test.todo("returns PASS when marketing lead has valid SOA and permission");
});
